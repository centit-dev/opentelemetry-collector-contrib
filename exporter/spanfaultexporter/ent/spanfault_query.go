// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/teanoon/opentelemetry-collector-contrib/exporter/spanfaultexporter/ent/predicate"
	"github.com/teanoon/opentelemetry-collector-contrib/exporter/spanfaultexporter/ent/spanfault"
)

// SpanFaultQuery is the builder for querying SpanFault entities.
type SpanFaultQuery struct {
	config
	ctx        *QueryContext
	order      []spanfault.OrderOption
	inters     []Interceptor
	predicates []predicate.SpanFault
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SpanFaultQuery builder.
func (sfq *SpanFaultQuery) Where(ps ...predicate.SpanFault) *SpanFaultQuery {
	sfq.predicates = append(sfq.predicates, ps...)
	return sfq
}

// Limit the number of records to be returned by this query.
func (sfq *SpanFaultQuery) Limit(limit int) *SpanFaultQuery {
	sfq.ctx.Limit = &limit
	return sfq
}

// Offset to start from.
func (sfq *SpanFaultQuery) Offset(offset int) *SpanFaultQuery {
	sfq.ctx.Offset = &offset
	return sfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sfq *SpanFaultQuery) Unique(unique bool) *SpanFaultQuery {
	sfq.ctx.Unique = &unique
	return sfq
}

// Order specifies how the records should be ordered.
func (sfq *SpanFaultQuery) Order(o ...spanfault.OrderOption) *SpanFaultQuery {
	sfq.order = append(sfq.order, o...)
	return sfq
}

// First returns the first SpanFault entity from the query.
// Returns a *NotFoundError when no SpanFault was found.
func (sfq *SpanFaultQuery) First(ctx context.Context) (*SpanFault, error) {
	nodes, err := sfq.Limit(1).All(setContextOp(ctx, sfq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{spanfault.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sfq *SpanFaultQuery) FirstX(ctx context.Context) *SpanFault {
	node, err := sfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SpanFault ID from the query.
// Returns a *NotFoundError when no SpanFault ID was found.
func (sfq *SpanFaultQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = sfq.Limit(1).IDs(setContextOp(ctx, sfq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{spanfault.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sfq *SpanFaultQuery) FirstIDX(ctx context.Context) string {
	id, err := sfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SpanFault entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SpanFault entity is found.
// Returns a *NotFoundError when no SpanFault entities are found.
func (sfq *SpanFaultQuery) Only(ctx context.Context) (*SpanFault, error) {
	nodes, err := sfq.Limit(2).All(setContextOp(ctx, sfq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{spanfault.Label}
	default:
		return nil, &NotSingularError{spanfault.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sfq *SpanFaultQuery) OnlyX(ctx context.Context) *SpanFault {
	node, err := sfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SpanFault ID in the query.
// Returns a *NotSingularError when more than one SpanFault ID is found.
// Returns a *NotFoundError when no entities are found.
func (sfq *SpanFaultQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = sfq.Limit(2).IDs(setContextOp(ctx, sfq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{spanfault.Label}
	default:
		err = &NotSingularError{spanfault.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sfq *SpanFaultQuery) OnlyIDX(ctx context.Context) string {
	id, err := sfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SpanFaults.
func (sfq *SpanFaultQuery) All(ctx context.Context) ([]*SpanFault, error) {
	ctx = setContextOp(ctx, sfq.ctx, "All")
	if err := sfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SpanFault, *SpanFaultQuery]()
	return withInterceptors[[]*SpanFault](ctx, sfq, qr, sfq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sfq *SpanFaultQuery) AllX(ctx context.Context) []*SpanFault {
	nodes, err := sfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SpanFault IDs.
func (sfq *SpanFaultQuery) IDs(ctx context.Context) (ids []string, err error) {
	if sfq.ctx.Unique == nil && sfq.path != nil {
		sfq.Unique(true)
	}
	ctx = setContextOp(ctx, sfq.ctx, "IDs")
	if err = sfq.Select(spanfault.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sfq *SpanFaultQuery) IDsX(ctx context.Context) []string {
	ids, err := sfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sfq *SpanFaultQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sfq.ctx, "Count")
	if err := sfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sfq, querierCount[*SpanFaultQuery](), sfq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sfq *SpanFaultQuery) CountX(ctx context.Context) int {
	count, err := sfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sfq *SpanFaultQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sfq.ctx, "Exist")
	switch _, err := sfq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sfq *SpanFaultQuery) ExistX(ctx context.Context) bool {
	exist, err := sfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SpanFaultQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sfq *SpanFaultQuery) Clone() *SpanFaultQuery {
	if sfq == nil {
		return nil
	}
	return &SpanFaultQuery{
		config:     sfq.config,
		ctx:        sfq.ctx.Clone(),
		order:      append([]spanfault.OrderOption{}, sfq.order...),
		inters:     append([]Interceptor{}, sfq.inters...),
		predicates: append([]predicate.SpanFault{}, sfq.predicates...),
		// clone intermediate query.
		sql:  sfq.sql.Clone(),
		path: sfq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Timestamp time.Time `json:"Timestamp,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SpanFault.Query().
//		GroupBy(spanfault.FieldTimestamp).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sfq *SpanFaultQuery) GroupBy(field string, fields ...string) *SpanFaultGroupBy {
	sfq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SpanFaultGroupBy{build: sfq}
	grbuild.flds = &sfq.ctx.Fields
	grbuild.label = spanfault.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Timestamp time.Time `json:"Timestamp,omitempty"`
//	}
//
//	client.SpanFault.Query().
//		Select(spanfault.FieldTimestamp).
//		Scan(ctx, &v)
func (sfq *SpanFaultQuery) Select(fields ...string) *SpanFaultSelect {
	sfq.ctx.Fields = append(sfq.ctx.Fields, fields...)
	sbuild := &SpanFaultSelect{SpanFaultQuery: sfq}
	sbuild.label = spanfault.Label
	sbuild.flds, sbuild.scan = &sfq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SpanFaultSelect configured with the given aggregations.
func (sfq *SpanFaultQuery) Aggregate(fns ...AggregateFunc) *SpanFaultSelect {
	return sfq.Select().Aggregate(fns...)
}

func (sfq *SpanFaultQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sfq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sfq); err != nil {
				return err
			}
		}
	}
	for _, f := range sfq.ctx.Fields {
		if !spanfault.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sfq.path != nil {
		prev, err := sfq.path(ctx)
		if err != nil {
			return err
		}
		sfq.sql = prev
	}
	return nil
}

func (sfq *SpanFaultQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SpanFault, error) {
	var (
		nodes = []*SpanFault{}
		_spec = sfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SpanFault).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SpanFault{config: sfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (sfq *SpanFaultQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sfq.querySpec()
	_spec.Node.Columns = sfq.ctx.Fields
	if len(sfq.ctx.Fields) > 0 {
		_spec.Unique = sfq.ctx.Unique != nil && *sfq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sfq.driver, _spec)
}

func (sfq *SpanFaultQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(spanfault.Table, spanfault.Columns, sqlgraph.NewFieldSpec(spanfault.FieldID, field.TypeString))
	_spec.From = sfq.sql
	if unique := sfq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sfq.path != nil {
		_spec.Unique = true
	}
	if fields := sfq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, spanfault.FieldID)
		for i := range fields {
			if fields[i] != spanfault.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sfq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sfq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sfq *SpanFaultQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sfq.driver.Dialect())
	t1 := builder.Table(spanfault.Table)
	columns := sfq.ctx.Fields
	if len(columns) == 0 {
		columns = spanfault.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sfq.sql != nil {
		selector = sfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sfq.ctx.Unique != nil && *sfq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sfq.predicates {
		p(selector)
	}
	for _, p := range sfq.order {
		p(selector)
	}
	if offset := sfq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sfq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SpanFaultGroupBy is the group-by builder for SpanFault entities.
type SpanFaultGroupBy struct {
	selector
	build *SpanFaultQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sfgb *SpanFaultGroupBy) Aggregate(fns ...AggregateFunc) *SpanFaultGroupBy {
	sfgb.fns = append(sfgb.fns, fns...)
	return sfgb
}

// Scan applies the selector query and scans the result into the given value.
func (sfgb *SpanFaultGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sfgb.build.ctx, "GroupBy")
	if err := sfgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SpanFaultQuery, *SpanFaultGroupBy](ctx, sfgb.build, sfgb, sfgb.build.inters, v)
}

func (sfgb *SpanFaultGroupBy) sqlScan(ctx context.Context, root *SpanFaultQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sfgb.fns))
	for _, fn := range sfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sfgb.flds)+len(sfgb.fns))
		for _, f := range *sfgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sfgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sfgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SpanFaultSelect is the builder for selecting fields of SpanFault entities.
type SpanFaultSelect struct {
	*SpanFaultQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sfs *SpanFaultSelect) Aggregate(fns ...AggregateFunc) *SpanFaultSelect {
	sfs.fns = append(sfs.fns, fns...)
	return sfs
}

// Scan applies the selector query and scans the result into the given value.
func (sfs *SpanFaultSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sfs.ctx, "Select")
	if err := sfs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SpanFaultQuery, *SpanFaultSelect](ctx, sfs.SpanFaultQuery, sfs, sfs.inters, v)
}

func (sfs *SpanFaultSelect) sqlScan(ctx context.Context, root *SpanFaultQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sfs.fns))
	for _, fn := range sfs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sfs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
