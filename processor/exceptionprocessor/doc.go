// parse exception from log and span and add it as an attribute exception.name
// to spans' and logs' attributes.
// and then access an external configuration database
// to classify the exception based on attributes and record its value as exception.category.
package exceptionprocessor
